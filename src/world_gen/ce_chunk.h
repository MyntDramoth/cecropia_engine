#pragma once
#include <glm/glm.hpp>
#include "svo.h"

class CE_Chunk {

   public:
    // CE_Chunk(glm::vec3 position);
    
    // glm::vec3 half_size {32,32,32};
    // void generate_chunk(glm::vec3 position, SVO *octree);


    private:
    
    //AABB default_bounds;
    //SVO tree {default_bounds,half_size.x * 2};
};