#pragma once

#include <inttypes.h>
//sets it so that that name on the left corresponds to the type on the right

typedef uint8_t u8;
typedef uint16_t u16;
typedef uint32_t u32;
typedef uint64_t u_longlong;

typedef int8_t i8;
typedef int16_t i16;
typedef int32_t i32;
typedef int64_t i_longlong;

typedef float f32;
typedef double f64;

typedef size_t usize;

